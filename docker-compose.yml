services:
  db:
    image: postgres:16
    container_name: lighthouse-db
    environment:
      POSTGRES_DB: lighthouse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: lighthouse123
    ports:
      - "5432:5432"
    volumes:
      - lighthouse_data:/var/lib/postgresql/data

volumes:
  lighthouse_data: